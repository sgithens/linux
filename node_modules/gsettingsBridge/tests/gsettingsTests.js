/*!
XML Settings Handler Tests

Copyright 2012 Raising the Floor - International

Licensed under the New BSD license. You may not use this file except in
compliance with this License.

You may obtain a copy of the License at
https://github.com/gpii/universal/LICENSE.txt
*/

/*global require, __dirname*/

(function () {
    // This loads universal.
        var fluid = require("universal"),
            gpii = fluid.registerNamespace("gpii"),
            jqUnit = fluid.require("jqUnit"),
            child_process = require("child_process"),
            fs = require("fs");
    fluid.require("gsettingsBridge", require);

    var gsettingsHandlerTests = gpii.tests.testEnvironment();

    // var addSchema = function () {
    //     var  cmd = "export GSETTINGS_SCHEMA_DIR='"+__dirname+"/data' && glib-compile-schemas '"+__dirname+"/data'";
    //     child_process.exec(cmd);
    //     var tmp = child_process.exec("gsettings list-keys net.gpii.testing.gsettings", function (error, stdout, stderr) {
    //         console.log('stdout: ' + stdout);
    //         console.log('stderr: ' + stderr);
    //         if (error !== null) {
    //           console.log('exec error: ' + error);
    //         }
    //     });
    
    //     //console.log(tmp);
    // };

    var tmper = function() {
        console.log("=========================================== HERE WE GO ============================================");
        gpii.gsettings.new(__dirname+"/data", "net.gpii.testing.gsettings");
        console.log("====================================================================================================");
    }

    tmper();
}());